sudo: false
language: node_js

env:
  - WEBPACK_VERSION="latest"
  - WEBPACK_VERSION="4.39.3"
node_js:
  - "lts/*"
  - "10"
  - "8"

before_install:
  - nvm --version
  - node --version
install: "yarn install --frozen-lockfile --ignore-engines"
before_script:
  - yarn add webpack@$WEBPACK_VERSION --ignore-engines
script:
  - yarn run test

jobs:
  include:
    - stage: release
      node_js: "lts/*"
      deploy:
        provider: script
        skip_cleanup: true
        script: yarn semantic-release
